# Аудит выполненной реализации: доработки по запросу клиента (март 2026)

**Дата аудита:** 2026-03-10  
**План:** Росток доработки клиент (переименование статусов, split-пайплайн, промпты, колонка P, документация).

---

## 1. Соответствие плану

| Пункт плана | Статус | Комментарий |
|-------------|--------|-------------|
| Переименование «Одобрено» → «Одобрено на публикацию» | Выполнено | types, tasks, writer, generation, publishing, scheduler, app_script, ПРОМПТ, РУКОВОДСТВО |
| Переименование «Согласовано» → «Согласован заголовок» | Выполнено | те же файлы + проверка в generation (вход в пайплайн) |
| Новый статус «Текст готов, ждём картинку» | Выполнено | TaskStatus, VALID_STATUSES, dropdown, imageGeneration, scheduler |
| Разделение пайплайна: текст без time-gate, картинка после generationTime | Выполнено | generation.ts только текст + writeTextResult; imageGeneration.ts + scheduler с canRunImageGeneration |
| Исправление кавычек/префикса заголовка в draft | Выполнено | Промпт без кавычек, явный запрет префикса; cleanArticleFirstLine в utils/text.ts; вызов в generation.ts |
| Улучшение очеловечивания (burstiness, шаблоны, списки) | Выполнено | Дефолтный промпт в humanize.ts расширен; дефолт в draft.ts дополнен |
| Вариативность интро/представлений | Выполнено | Инструкции в humanize и draft (дефолты) |
| Автозаполнение колонки P «Символов» | Выполнено | writeTextResult, writeGenerationResult пишут P; insertTaskRows добавляет 0 в P, диапазон A:P; заголовок в app_script |
| Обновление app_script и документации | Выполнено | РУКОВОДСТВО, ПРОМПТ; пояснение про роль; новый пайплайн описан |

---

## 2. Согласованность кода

- **Типы и валидация:** TaskStatus и VALID_STATUSES содержат одни и те же 11 статусов, порядок совпадает.
- **Поток статусов:** Согласован заголовок → (generationPipeline) → Текст готов, ждём картинку → (imageGenerationPipeline после generationTime) → Готово к проверке / Одобрено на публикацию. «На доработку» → тот же generationPipeline (только текст) → Текст готов, ждём картинку → далее картинка по времени.
- **Публикация:** Проверка `task.status === 'Одобрено на публикацию'` в publishing.ts и scheduler — консистентно.
- **Перегенерация картинки:** regenerateImagePipeline по-прежнему ставит «Готово к проверке» (хардкод); не переводит в «Одобрено на публикацию» при выключенной модерации — осознанное упрощение (в плане не требовалось менять regenerateImage).

---

## 3. Замечания и риски

### 3.1. writeGenerationResult

- **writeGenerationResult** в `writer.ts` после разделения пайплайна не вызывается из текущих пайплайнов. Текст пишется через **writeTextResult**, картинка — через **writeRegeneratedImage**. Функция оставлена с комментарием для возможного сценария «текст + картинка за один вызов» (тесты, ручной запуск).

### 3.2. Документация вне основного пакета правок

В следующих файлах по-прежнему используются старые формулировки статусов или старый сценарий генерации. На работу кода не влияют, но могут путать при чтении:

| Файл | Статус |
|------|--------|
| `content-factory/ДЕПЛОЙ_VPS.md` | Обновлено: «Время генерации» для «Текст готов, ждём картинку» (картинка), текст — сразу при «Согласован заголовок» |
| `content-factory/README.md` | Обновлено: «Согласован заголовок», двухшаговый поток (текст → «Текст готов, ждём картинку» → картинка после 05:00) |
| `content-factory/ТЕСТИРОВАНИЕ.md` | Обновлено: все статусы, таблица пайплайнов, пошаговый сценарий с текстом и картинкой |
| `content-factory/АУДИТ_КОДА.md` | Обновлено: формулировки статусов и учёт в imageGeneration |
| `content-factory/docs/AUDIT_ТРИ_ИСПРАВЛЕНИЯ.md` | Обновлено: время генерации для картинки, «Согласован заголовок» без time-gate |
| `ПРОМПТ_для_создания_Google_таблицы.md` | Уточнено: статус «Текст готов, ждём картинку» в кавычках в пояснении |
| `ПЛАН_РЕАЛИЗАЦИИ.md`, ТЗ v3/v4 | Исторические; при необходимости обновить по желанию заказчика |

### 3.3. ПРОМПТ_для_создания_Google_таблицы.md

- В пояснении к списку статусов добавлено уточнение: «Текст готов, ждём картинку» (один статус, с запятой внутри) — в кавычках в тексте промпта.

### 3.4. Таблицы, созданные до правок

- В существующих Google Таблицах колонка P может отсутствовать. Запись в P (например, `'Задания'!P2`) при первом обращении создаёт колонку — поведение Google Sheets нормальное.
- Выпадающий список в колонке E нужно обновить (перезапуск Apps Script с новым списком статусов или ручное обновление Data Validation). Старые ячейки со «Согласовано»/«Одобрено» не будут распознаны кодом (VALID_STATUSES), строки выпадут из выборки до ручной замены статусов в таблице.

### 3.5. Пост-обработка первой строки

- `cleanArticleFirstLine` обрабатывает только первую строку: снимает префикс `Заголовок:` / `{Заголовок}:` и обрамляющие кавычки. Варианты вроде «Заголовок статьи: Текст» обрабатываются корректно; экзотические форматы (например, заголовок на второй строке) не нормализуются — при необходимости можно расширить логику.

---

## 4. Покрытие тестами

- Добавлен `src/utils/text.test.ts`: тесты для `cleanArticleFirstLine` (префикс, кавычки, без изменений, пустая строка, регистр) и для `truncateAtSentence`. Все проходят.

---

## 5. Итог

Реализация соответствует плану: статусы переименованы, пайплайн разделён на текст (без time-gate) и картинку (после generationTime), промпты и пост-обработка заголовка поправлены, колонка P заполняется автоматически, основная документация (РУКОВОДСТВО, ПРОМПТ, app_script) обновлена. Критичных расхождений с планом нет.

По результатам доводки: документация (ДЕПЛОЙ_VPS, README, ТЕСТИРОВАНИЕ, АУДИТ_КОДА, AUDIT_ТРИ_ИСПРАВЛЕНИЯ) обновлена; в ПРОМПТ уточнён статус с запятой; для `writeGenerationResult` добавлен комментарий (оставлена на будущее); добавлены тесты для `cleanArticleFirstLine`. После деплоя проверить на реальной таблице обновление Data Validation и замену старых статусов.
